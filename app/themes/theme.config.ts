import type { ThemeConfig } from "antd";

const theme: ThemeConfig = {
  token: {
    colorPrimary: "#4572c1",// Corresponds to @primary-color
     colorInfo: "#80d0f7",     // Sky Blue for lighter elements like inputs
    colorBgLayout: "#f0f0f0"
  },
};
export default theme;
